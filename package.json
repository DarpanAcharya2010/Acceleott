{
  "name": "acceleott",
  "version": "1.0.0",
  "private": true,
  "description": "Unified deployment for Acceleott frontend (React) and backend (Express + MongoDB).",
  "scripts": {
    "build": "cd acceleott-mueller && npm install && npm run build",
    "start": "node acceleott-backend/server.js"
  },
  "dependencies": {
    "axios": "^1.11.0",
    "mongodb": "4.1",
    "react-router-dom": "^7.8.2"
  },
  "engines": {
    "node": ">=18.0.0"
  },
  "license": "MIT"
}
